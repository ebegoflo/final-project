var message = (prompt('Ingresa tu mensage a cifrar')).toUpperCase(); // convertir a mayuculas la entrada del prompt, para aceptar minusculas y myusculas

function cipher( message){
  if (message.length === 0 || typeof phrase === isNaN) {
    alert('Información no valida')
  }

}
  cipher(message);
